services:
  - type: web
    name: ram-ai-portfolio
    runtime: node
    plan: free
    buildCommand: echo "Zero npm dependencies - build complete!"
    startCommand: node server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
